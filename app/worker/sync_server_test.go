package worker

import (
	"testing"
)

func TestServerSyncExecute(t *testing.T) {
	// TODO mock ssh server? create server in db, create keys, run sync, validate from mock server
	// server := &models.Record{Host: "localhost", Port: 22, Username: "root", Password: "password"}
	// work := &SyncServerWork{Server: server}
}
